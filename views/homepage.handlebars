{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{post.post_title}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{post.post_text}}">{{post.post_text}}</a></h3>
  </div>
</div>
{{/each}}
